{
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\" --ignore-path .gitignore"
  },
  "devDependencies": {
    "@types/minimatch": "^6.0.0",
    "@types/node": "^25.5.2",
    "prettier": "^3.1.1",
    "prisma": "^7.6.0",
    "turbo": "^2.9.3"
  },
  "engines": {
    "node": ">=18"
  },
  "name": "with-react-native-web",
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@edu-platform/core": "workspace:*",
    "@edu-platform/infrastructure": "workspace:*",
    "@prisma/client": "^7.6.0",
    "zod": "^4.3.6"
  }
}
